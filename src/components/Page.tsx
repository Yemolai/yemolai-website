import styled from 'styled-components'

export const Page = styled.section`
  background: white;
  border: solid rgba(255, 255, 255, 0.1);
  border-radius: 2px;
  box-shadow: 3px 4px 8px -2px rgba(12, 12, 12, 0.3);
  display: flex;
  flex-flow: column nowrap;
  max-width: 8.25in; // A4's width
  min-height: 11.75in; // A4's height
  padding: 0.5in 0.4in;
`
